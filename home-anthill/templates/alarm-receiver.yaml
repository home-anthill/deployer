---
apiVersion: v1
kind: Secret
metadata:
  name: alarm-receiver-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  .env: |-
    LOG_LEVEL=info
    MONGODB_URL={{ .Values.mongodbUrl }}
    ONLINE_REDIS_URI=redis://{{ .Values.redis.serviceName }}.{{ .Values.namespace }}.svc.cluster.local:6379/0
    REPLAY_REDIS_URI=redis://{{ .Values.redis.serviceName }}.{{ .Values.namespace }}.svc.cluster.local:6379/2
    ALARMS_REDIS_URI=redis://{{ .Values.redis.serviceName }}.{{ .Values.namespace }}.svc.cluster.local:6379/3
    REDIS_USERNAME={{ .Values.redis.username }}
    REDIS_PASSWORD={{ .Values.redis.password }}
    API_TOKEN_HASH_SECRET={{ .Values.apiToken.hashSecret }}
    API_TOKEN_ENCRYPTION_KEY={{ .Values.apiToken.encryptionKey }}
    MQTT_URL={{ .Values.mosquitto.serviceName }}.{{ .Values.namespace }}.svc.cluster.local
    MQTT_PORT=1883
    MQTT_TLS=false
    ROOT_CA=
    MQTT_CERT_FILE=
    MQTT_KEY_FILE=
    MQTT_CLIENT_ID=alarm-receiver
    {{- if .Values.mosquitto.auth.enable }}
    MQTT_AUTH=true
    MQTT_USER={{ .Values.mosquitto.auth.users.alarmReceiver.username }}
    MQTT_PASSWORD={{ .Values.mosquitto.auth.users.alarmReceiver.password }}
    {{ else }}
    MQTT_AUTH=false
    MQTT_USER=
    MQTT_PASSWORD=
    {{ end }}
  Rocket.toml: |-
    [default]

    [release]
    address = "0.0.0.0"
    port = 80
    secret_key="{{ .Values.alarmReceiver.rocketSecretKey.release }}"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alarm-receiver
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: alarm-receiver
  template:
    metadata:
      labels:
        app: alarm-receiver
    spec:
      {{- if .Values.serviceAccounts.enabled }}
      serviceAccountName: alarm-receiver
      automountServiceAccountToken: false
      {{- end }}
      {{- if .Values.dhi.enabled }}
      imagePullSecrets:
        - name: dhi-registry-secret
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      {{- if .Values.domains.mqtt.ssl.enable }}
      initContainers:
        - name: wait-for-mqtt
          image: {{ if .Values.dhi.enabled }}{{ .Values.alpine.hardenedImage }}{{ else }}{{ .Values.alpine.image }}{{ end }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            {{- toYaml .Values.alpine.resources | nindent 12 }}
          command:
            - "/bin/sh"
            - "-c"
            - |
              until nc -z {{ .Values.mosquitto.serviceName }}.{{ .Values.namespace }}.svc.cluster.local 1883; do
                echo "Waiting for Mosquitto on port 1883..."; sleep 2
              done
      {{- end }}
      containers:
      - name: alarm-receiver
        image: {{ .Values.alarmReceiver.image }}
        {{- if .Values.debug.pods.alwaysPullContainers }}
        imagePullPolicy: Always
        {{- end }}
        {{- if .Values.debug.pods.sleepInfinity }}
        command: ["/bin/sh"]
        args: ["-c", "chmod +x /app/alarm-receiver; /app/alarm-receiver; sleep Infinity;"]
        {{- end }}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: ["ALL"]
            add: ["NET_BIND_SERVICE"]
        resources:
          {{- toYaml .Values.alarmReceiver.resources | nindent 10 }}
        readinessProbe:
          httpGet:
            path: /keepalive
            port: http
          periodSeconds: 30
        livenessProbe:
          httpGet:
            path: /keepalive
            port: http
          periodSeconds: 60
          failureThreshold: 2
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        volumeMounts:
        - name: alarm-receiver-secret
          mountPath: /app/.env
          subPath: .env
          readOnly: true
        - name: alarm-receiver-secret
          mountPath: /app/Rocket.toml
          subPath: Rocket.toml
          readOnly: true
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: alarm-receiver-secret
        secret:
          secretName: alarm-receiver-secret
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}
